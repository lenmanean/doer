'use client'

/**
 * Analytics Scripts Component
 * 
 * Note: Only essential cookies are collected. Analytics and marketing scripts are disabled.
 * This component returns null to ensure no tracking scripts are loaded.
 */
export function AnalyticsScripts() {
  // Only essential cookies are used - analytics and marketing scripts are disabled
  // No analytics or marketing tracking scripts are loaded
  return null
}

