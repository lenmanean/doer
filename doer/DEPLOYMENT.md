# Deployment Configuration

This Next.js application is located in the `doer` directory.

## Vercel Configuration Required

- **Root Directory**: `doer`
- **Framework**: Next.js (auto-detected)
- **Node Version**: 22.x

## Verification

- ✅ `package.json` exists in `doer/` directory
- ✅ Next.js is in dependencies: `"next": "^14.2.5"`
- ✅ `next.config.mjs` exists
- ✅ `src/app/` directory exists (Next.js App Router)

## Build Commands

- Install: `npm install` (run from `doer/` directory)
- Build: `npm run build` (run from `doer/` directory)
- Start: `npm start` (run from `doer/` directory)

